{
  "name": "edge-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vocs dev",
    "docs:build": "vocs build",
    "docs:preview": "vocs preview",
    "docs:validate": "node bin/validate-vocs-docs.mjs",
    "docs:check": "npm run docs:build && npm run docs:validate"
  },
  "dependencies": {
    "vocs": "1.4.1"
  }
}
